import DefaultPageContainer from '../../templates/DefaultPageContainer'

import styled from 'styled-components'

const SkillSection = () => {
    return <DefaultPageContainer>oi</DefaultPageContainer>
}

export default SkillSection
